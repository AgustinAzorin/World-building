{
  "name": "@world-building/combat",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@world-building/domain": "workspace:*",
    "@world-building/shared": "workspace:*"
  }
}
